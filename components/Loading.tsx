"use client";

import React from "react";

/**
 * Simple loading indicator. Can be replaced with a spinner or skeleton
 * component if desired. For now it just renders a text message.
 */
export default function Loading() {
  return <p className="text-gray-500">Loading...</p>;
}